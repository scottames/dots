# Show desktop notifications when the YubiKey is waiting for touch.
YUBIKEY_TOUCH_DETECTOR_LIBNOTIFY=true

# Keep the upstream socket enabled for status bar integrations.
YUBIKEY_TOUCH_DETECTOR_NOSOCKET=false

YUBIKEY_TOUCH_DETECTOR_STDOUT=false
